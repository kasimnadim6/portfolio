import styles from './FunProjects.module.scss';

const FunProjects = () => {
  return (
    <section className={styles['fun-projects']}>
      <h2 className={styles.heading}>Latest Works</h2>
    </section>
  );
};

export default FunProjects;
